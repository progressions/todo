source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem "todoable", git: "https://github.com/progressions/todoable", branch: "token"

# Specify your gem's dependencies in todo.gemspec
gemspec
